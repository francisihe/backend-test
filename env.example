
# Do not use both. Use either the DATABASE_URL or the other individual DB settings

DATABASE_URL=''

DB_HOST='localhost'
DB_NAME='database-name'
DB_USER='database-user'
DB_PASSWORD='database-password'
DB_PORT='5432'

PORT=3000

JWT_SECRET='jwt-secret'